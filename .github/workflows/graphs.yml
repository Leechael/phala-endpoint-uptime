# This file was generated by Leechael/phala-endpoint-uptime@ac96b730ba5321711bc07a3463838306af6360c1
#
# ===============================
# Do not edit this file directly!
# ===============================
#
# Your changes will be overwritten when the template updates (daily)
# Instead, change your .upptimerc.yml configuration: https://upptime.js.org/docs

name: Graphs CI
on:
  schedule:
    - cron: "0 0 * * *"
  repository_dispatch:
    types: [graphs]
  workflow_dispatch:
jobs:
  release:
    name: Generate graphs
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.3
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.GH_PAT }}
      - name: Generate graphs
        uses: Leechael/phala-endpoint-uptime@ac96b730ba5321711bc07a3463838306af6360c1
        with:
          command: "graphs"
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
